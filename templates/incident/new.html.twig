{% extends 'base.html.twig' %}

{% block title %}Déclarer un Incident — GESTLOYER{% endblock %}

{% block body %}
<div class="min-h-screen themed-page flex items-center justify-center py-12 bg-slate-50 dark:bg-white dark:bg-slate-900 text-slate-800 dark:text-slate-100">
    <div class="w-full max-w-lg px-4">
        <div class="rounded-2xl p-8 bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 shadow-xl dark:shadow-none">
            <div class="text-center mb-8">
                <div class="text-5xl mb-4">🔔</div>
                <h1 class="text-2xl font-bold text-slate-900 dark:text-white">Déclarer un Incident</h1>
                <p class="text-slate-500 dark:text-slate-600 dark:text-slate-400 text-sm mt-1">Signalez un problème sur un bien</p>
            </div>

            <form method="POST" action="{{ path('app_incident_new') }}" class="space-y-5">
                <input type="hidden" name="_token" value="{{ csrf_token('incident_new') }}">

                <div>
                    <label class="block text-sm font-medium text-slate-700 dark:text-slate-700 dark:text-slate-300 mb-2">Titre *</label>
                    <input type="text" name="titre" required maxlength="255"
                           class="w-full px-4 py-3 rounded-xl text-sm bg-white dark:bg-slate-800 border border-slate-300 dark:border-slate-600 text-slate-900 dark:text-white"
                           placeholder="Ex: Fuite d'eau dans la cuisine">
                </div>

                <div>
                    <label class="block text-sm font-medium text-slate-700 dark:text-slate-700 dark:text-slate-300 mb-2">Description *</label>
                    <textarea name="description" required rows="4"
                              class="w-full px-4 py-3 rounded-xl text-sm resize-none bg-white dark:bg-slate-800 border border-slate-300 dark:border-slate-600 text-slate-900 dark:text-white"
                              placeholder="Décrivez l'incident en détail..."></textarea>
                </div>

                <div>
                    <label class="block text-sm font-medium text-slate-700 dark:text-slate-700 dark:text-slate-300 mb-2">Bien concerné *</label>
                    <select name="bien_id" required
                            class="w-full px-4 py-3 rounded-xl text-sm bg-white dark:bg-slate-800 border border-slate-300 dark:border-slate-600 text-slate-900 dark:text-white">
                        <option value="">— Sélectionner —</option>
                        {% for bien in biens %}
                            <option value="{{ bien.id }}">{{ bien.adresse }}</option>
                        {% endfor %}
                    </select>
                </div>

                <div>
                    <label class="block text-sm font-medium text-slate-700 dark:text-slate-700 dark:text-slate-300 mb-2">Priorité *</label>
                    <div class="grid grid-cols-3 gap-2">
                        {% for p in priorites %}
                        {% set p_colors = {'URGENT': '#ef4444', 'NORMAL': '#f59e0b', 'FAIBLE': '#10b981'} %}
                        {% set pc = p_colors[p.value] ?? '#64748b' %}
                        <label class="cursor-pointer">
                            <input type="radio" name="priorite" value="{{ p.value }}" class="sr-only"
                                   {% if p.value == 'NORMAL' %}checked{% endif %}>
                            <div class="px-3 py-2 rounded-xl text-center text-sm font-medium transition border-2 text-slate-900 dark:text-white"
                                 style="border-color: {{ pc }}44; background: {{ pc }}22; color: {{ pc }};">
                                {% if p.value == 'URGENT' %}🚨{% elseif p.value == 'NORMAL' %}⚠️{% else %}ℹ️{% endif %}
                                {{ p.value }}
                            </div>
                        </label>
                        {% endfor %}
                    </div>
                </div>

                <div class="flex gap-3 pt-2">
                    <a href="{{ path('app_incident_index') }}"
                       class="flex-1 px-4 py-3 rounded-xl text-sm font-medium text-center text-slate-700 dark:text-slate-700 dark:text-slate-300 bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 shadow-sm">
                        Annuler
                    </a>
                    <button type="submit"
                            class="flex-1 px-4 py-3 rounded-xl text-sm font-medium text-slate-900 dark:text-white"
                            style="background: linear-gradient(135deg, #ef4444, #db2777);">
                        📢 Déclarer
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
{% endblock %}
